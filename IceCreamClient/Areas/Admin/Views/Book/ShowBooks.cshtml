@model IEnumerable<IceCreamClient.Models.BookIceCream>

@{
    ViewData["Title"] = "ShowBooks";
}

<!-- page title area start -->
<div class="page-title-area">
    <div class="row align-items-center">
        <div class="col-sm-6">
            <div class="breadcrumbs-area clearfix">
                <ul class="breadcrumbs pull-left">
                    <li><a asp-action="ShowBooks" asp-controller="Book">Book</a></li>
                    <li><span>View List</span></li>
                </ul>
            </div>
        </div>
    </div>
</div>
<!-- page title area end -->
<div class="main-content-inner">
    <div class="row">
        <!-- data table start -->
        <div class="col-12 mt-5">
            <div class="card">
                <div class="card-body">
                    <h4 class="header-title">Books List</h4>
                    <div class="data-tables">
                        <table id="dataTable3" class="table text-center">
                            <thead class="bg-light text-capitalize">
                                <tr style="max-width:50px; word-wrap:normal">
                                    <th>No.</th>
                                    <th>Title</th>
                                    <th>Author</th>
                                    <th>Price($)</th>
                                    <th>Image</th>
                                    <th>Status</th>
                                    <th>Create At</th>
                                    <th>Update | Details</th>
                                </tr>
                            </thead>
                            <tbody>
                                @foreach (var item in Model)
                                {
                                    <tr style="max-width:50px; word-wrap:normal">
                                        <td>@item.BookId</td>
                                        <td>@item.Title</td>
                                        <td>@item.Author</td>
                                        <td>@item.Price</td>
                                        <td>
                                            @if (item.Image != null)
                                            {
                                                <img src="~/images/book/@item.Image" alt="image" style="width:100px; height:100px;" />
                                            }
                                            else
                                            {
                                                @: No Image
                                            }
                                        </td>
                                        <td>
                                            @if (item.Active == true)
                                            {
                                                <a href="@Url.ActionLink("UpdateStatus", "Book", new { bookId = item.BookId, status = false })" class="btn-success">Yes</a>
                                            }
                                            else
                                            {
                                                <a href="@Url.ActionLink("UpdateStatus", "Book", new { bookId = item.BookId, status = true })" class="btn-danger">No</a>
                                            }
                                        </td>
                                        <td>
                                            @item.CreateAt
                                    </td>
                                    <td>
                                        @Html.ActionLink("Update", "UpdateBook", new { bookId = item.BookId }) |
                                        @Html.ActionLink("Details", "BookDetails", new { bookId = item.BookId })
                                    </td>
                                </tr>
                            }

                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>
        <!-- data table end -->
    </div>
</div>
<!-- main content area end -->